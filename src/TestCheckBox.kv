<TextChanger>
    orientation: 'vertical'
    BoxLayout:
        CheckBox:
            active: root.check
            on_press: root.checkbox_check(self)
        CheckBox:
            active: root.check
            on_press: root.checkbox_check(self)
        